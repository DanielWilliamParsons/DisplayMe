window.addEventListener('load', () => {

    /**
     * DISPLAY DRAWINGS
     */

    const displayDrawings = () => {
        for (let i = 0; i < displayMe.length; i++) {
            console.log(displayMe[i].picSrc);
        }
    }
    displayDrawings();

});